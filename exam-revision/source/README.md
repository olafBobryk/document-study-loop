# Local Source Documents

Place raw PDFs, slide decks, sample exams, archives, or other private source documents here when working locally.

This folder is intentionally ignored by Git except for this README and `.gitkeep`. Agents should read local files from here when available, but should not commit raw source documents.
